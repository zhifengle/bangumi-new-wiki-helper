import { SubjectTools } from '../catalogTypes';
import { trimParenthesis } from '../core/trim';

export function dangdangTitle(str: string) {
  return trimParenthesis(str);
}

export const dangdangBookTools: SubjectTools = {
};

