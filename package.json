{
  "name": "bangumi-new-wiki-helper",
  "version": "0.5.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "sync:version": "node bin/sync-version.js",
    "prebuild": "npm run sync:version",
    "prebuild:script": "npm run sync:version",
    "prebuild:ext": "npm run sync:version",
    "predev:ext": "npm run sync:version",
    "prestart": "npm run sync:version",
    "prestart2": "npm run sync:version",
    "build": "web-ext build -s extension",
    "build:script": "rollup -c build/rollup.config.us.js",
    "build:ext": "rollup -c build/rollup.config.ext.js",
    "dev:ext": "rollup -w -c build/rollup.config.ext.js",
    "dev:test": "rollup -w -c build/rollup.config.dev.js",
    "clean": "rimraf dist/ extension/dist",
    "start2": "web-ext run --firefox-profile=default -s extension/",
    "start": "node bin/run-fx.js",
    "test": "vitest run",
    "test:e2e:live": "playwright test --config playwright.live.config.ts"
  },
  "author": "zhifengle",
  "license": "MPL-2.0",
  "devDependencies": {
    "@playwright/test": "1.59.1",
    "@rollup/plugin-commonjs": "11.0.2",
    "@rollup/plugin-multi-entry": "latest",
    "@rollup/plugin-node-resolve": "7.1.1",
    "@rollup/plugin-replace": "2.3.1",
    "@types/firefox-webext-browser": "120.0.4",
    "@types/webextension-polyfill": "0.12.3",
    "jsdom": "29.0.2",
    "rollup": "2.0.5",
    "rollup-plugin-terser": "5.3.0",
    "rollup-plugin-typescript2": "0.26.0",
    "ts-loader": "^9.2.3",
    "ts-node": "^10.9.0",
    "typescript": "^5.9.0",
    "vite": "8.0.8",
    "vitest": "4.1.3",
    "web-ext": "^8.9.0",
    "webextension-polyfill": "0.12.0"
  },
  "dependencies": {
    "notyf": "^3.10.0",
    "stackblur-canvas": "2.2.0"
  }
}
